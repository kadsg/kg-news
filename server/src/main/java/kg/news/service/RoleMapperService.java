package kg.news.service;

import kg.news.entity.RoleMapper;

public interface RoleMapperService {
    /**
     * 添加角色映射
     * @param roleMapper 角色映射
     */
    void addRoleMapper(RoleMapper roleMapper);
}